'use client';

import { IStepComponentProps } from '@/types/quiz/step';

import styles from './styles.module.scss';

export const GRAPH_KEY = 'graph';

const GraphStep = ({}: IStepComponentProps) => {
  return (
    <></>
  );
}

export default GraphStep;
