'use client';

import { IStepComponentProps } from '@/types/quiz/step';

import styles from './styles.module.scss';

export const CONTACT_FORM_KEY = 'contact-form';

const ContactFormStep = ({}: IStepComponentProps) => {
  return (
    <>Contact step</>
  );
}

export default ContactFormStep;
